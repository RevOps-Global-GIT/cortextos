# Retired: Daily Hero Verify

Retired 2026-06-16.

Do not run this prompt. The canonical daily hero path is the VM `ob1-daily-vignette` generator, scheduled in the dev agent config. The generator publishes only after its own guard passes; otherwise it holds the last good hero and alerts through the cron wrapper.
